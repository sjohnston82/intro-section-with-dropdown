"use client";

import { useIntroContext } from "@/app/context/Context";
import React from "react";
import { cn } from "../../utils";

const SlideInMenu = () => {
  const { navOpen, setNavOpen } = useIntroContext();
  return <div className={}>SlideInMenu</div>;
};

export default SlideInMenu;
